{{!-- codigo de EDIT  y el head con footer los colocara main.hbs --}}
<div class="col-md-4 mx-auto">

    <div class="card">
        <div class="card-body">
            {{!-- este codigo es similar al de add.hbs con ciertas variantes, en el form action se indica la
                  ruta adonde debe mostrarse: /links/edit/link.id,  esto viene desde link.js en el siguiente 
                  metodo router.get('/edit/:id', con la instruccion res.render('links/edit', {link: links[0]}); 
                   todo esto es en el servidor --}}
            <form action="/links/edit/{{link.id}}" method="POST">
                {{!-- La otra variante esta en el value de cada form-group donde se indica cada campo que es 
                      enviado desde link.js en el siguiente metodo router.get('/edit/:id', con la instruccion 
                      res.render('links/edit', {link: links[0]});
                   --}}
                <div class="form-group">
                    <input type="text" name="title" class="form-control" placeholder="Title" value="{{link.title}}" autofocus>
                </div>
                
                <div class="form-group">
                    <input type="url" name="url" class="form-control" placeholder="URL" value="{{link.url}}">
                </div>
                
                <div class="form-group">
                    <textarea name="description" rows="2" class="form-control" placeholder="Description">{{link.description}}</textarea>
                </div>
                
                <div class="form-group">
                    <button class="btn btn-success btn-block">
                        Save
                    </button>
                </div>

            </form>
        </div>
    </div>
</div>