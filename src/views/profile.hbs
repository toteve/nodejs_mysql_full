
{{!-- codigo de profile.hbs  que mostrara los datos del usuario al loguearse y autenticarse --}}

<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                {{!-- en profile.hbs  hago uso de variable global user declarada en index.js principal
                      y generada en el modulo passport con SerializeUser, de user tomo fullname y 
                      username --}}
                <div class="card-body">
                    <h3>Welcome {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    {{!-- defino que etiqueta <a nos llevara a ruta /links --}}

                    <a href="/links" class="btn btn-primary m-4">
                        Save your Links    
                    </a>

                </div>

            </div>
        </div>

    </div>
</div>